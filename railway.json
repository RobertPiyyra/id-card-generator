{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'gunicorn app:app --workers=1 --threads=2 --timeout 300 --graceful-timeout 300 --keep-alive 5 --bind 0.0.0.0:${PORT:-8080}'"
  }
}
